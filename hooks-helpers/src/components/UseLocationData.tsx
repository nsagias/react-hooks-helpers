import useLocationData from "../hooks/useLocationData";

const LocationData = () => {
  useLocationData
  return (
    <div>
      <h1>useLocationData</h1>
    </div>
  );
};

export default LocationData;